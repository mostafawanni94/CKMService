# Direct dependencies only. Transitive packages are resolved by pip, which is
# why this file no longer mirrors `pip freeze` — that made the list drift and
# hid the fact that openpyxl, reportlab, PyPDF2 and requests were imported by
# the code but never declared.
#
# Install with:  pip install -r requirements.txt

# --- Framework -------------------------------------------------------------
Django==6.0
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==25.2
drf-spectacular==0.29.0

# --- Configuration ---------------------------------------------------------
python-dotenv==1.2.1
tzdata==2025.3                 # zoneinfo database (Europe/Amsterdam)

# --- Media & documents -----------------------------------------------------
pillow==12.0.0                 # ImageField support
openpyxl==3.1.5                # worklog + expense Excel exports
reportlab==4.4.7               # employee document PDF generation
PyPDF2==3.0.1                  # PDF merging in employees/pdf_generator.py

# --- Push notifications ----------------------------------------------------
requests==2.32.5
google-auth==2.57.0            # OAuth2 for the FCM HTTP v1 API

# --- Optional: PostgreSQL (set DATABASE_ENGINE=postgresql) ------------------
# psycopg[binary]==3.2.3

# --- Optional: production WSGI server ---------------------------------------
# gunicorn==23.0.0
